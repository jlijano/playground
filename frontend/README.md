# Frontend Source

This folder is reserved for the React + Vite + Tailwind source app if the project uses a build step.

Production deployment must remain static. Built frontend assets should be copied or emitted into `public_html`.
