# Database Migrations

Place MySQL/MariaDB setup scripts here.

Rules:

- Use `utf8mb4`.
- Add indexes for common lookup fields.
- Use clear numbered filenames.
- Do not include real credentials in SQL files.
